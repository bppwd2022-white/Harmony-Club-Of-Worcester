<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @web_event_location.title %>
</p>

<<p>ALL LISTINGS</p>
<% @web_event_location.listings.each do |listing| %>
  <<p><%= listing.title %></p>
<% end %>

<%= link_to 'Edit', edit_web_event_location_path(@web_event_location) %> |
<%= link_to 'Back', web_event_locations_path %>
